name: CI Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: cd frontend && npm install
      - run: cd frontend && npm run build

      - run: cd backend && pip install fastapi uvicorn
